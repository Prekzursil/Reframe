# Local GPU workstation
#
# Usage:
#   cp infra/examples/env.local-gpu.example .env
#   make api-dev
#   make worker-dev
#   make web-dev
#
# Notes:
# - Keep offline-first by default.
# - Install local transcription + translation backends as needed:
#     pip install -e "packages/media-core[transcribe-faster-whisper,translate-local]"
#   Then in the UI choose:
#     backend=faster_whisper, device=cuda

DATABASE_URL=sqlite:///./reframe.db
MEDIA_ROOT=./media
BROKER_URL=redis://localhost:6379/0
RESULT_BACKEND=${BROKER_URL}

REFRAME_OFFLINE_MODE=true

VITE_API_BASE_URL=http://localhost:8000/api/v1

