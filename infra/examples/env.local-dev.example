# Local dev (no GPU)
#
# Usage:
#   cp infra/examples/env.local-dev.example .env
#   make api-dev
#   make worker-dev
#   make web-dev
#
# Requires a local Redis running on the host (or adjust BROKER_URL).

DATABASE_URL=sqlite:///./reframe.db
MEDIA_ROOT=./media
BROKER_URL=redis://localhost:6379/0
RESULT_BACKEND=${BROKER_URL}

# Offline-first by default (no network providers)
REFRAME_OFFLINE_MODE=true

# Web app settings
VITE_API_BASE_URL=http://localhost:8000/api/v1

