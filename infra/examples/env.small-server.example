# Small server deployment (Docker)
#
# Usage (Docker Compose):
#   cp infra/examples/env.small-server.example .env
#   make compose-up
#
# Usage (All-in-one image):
#   See README.md "All‑in‑one Docker image (API + worker)" for a `docker run` example.
#
# Notes:
# - This file prefers container-friendly defaults (redis hostname).
# - For production, prefer Postgres and a real reverse proxy.

DATABASE_URL=sqlite:////data/reframe.db
MEDIA_ROOT=/data/media
BROKER_URL=redis://redis:6379/0
RESULT_BACKEND=${BROKER_URL}

REFRAME_OFFLINE_MODE=true

# If you run the web app separately, point it at your server:
# VITE_API_BASE_URL=http://<server-host>:8000/api/v1

