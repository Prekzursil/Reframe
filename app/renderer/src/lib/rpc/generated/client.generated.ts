// @generated by sidecar/contract/generate.py — DO NOT EDIT BY HAND.
// Regenerate: `python -m contract.generate` (run from sidecar/).
// contract-source-sha256: 80da0c4d44e9de705c52d920c62fe5103be970b59f33b973992a8a12b2c3e75a
//
// Method-typed client wrappers over the existing rpc() runtime (POC slice).

import { rpc } from '../client';
import type { RevealKeyResult, Settings, ShortmakerControls, Video } from './schemas.generated';
import type { Candidate, JobHandle } from '../schemas';

export const clientGenerated = {
  ping: (): Promise<{ pong: boolean; version: string }> => rpc('ping'),

  library: {
    add: (path: string): Promise<{ video: Video }> => rpc('library.add', { path }),
  },

  providers: {
    revealKey: (id: string, index = 0): Promise<RevealKeyResult> =>
      rpc('providers.revealKey', { id, index }),
  },

  settings: {
    get: (): Promise<Settings> => rpc('settings.get'),
    set: (values: Partial<Settings>): Promise<Settings> => rpc('settings.set', values),
  },

  shortmaker: {
    select: (
      videoId: string,
      prompt: string,
      controls: ShortmakerControls,
    ): Promise<JobHandle & { candidates?: Candidate[] }> =>
      rpc('shortmaker.select', { videoId, prompt, controls }),
  },
} as const;

export default clientGenerated;
