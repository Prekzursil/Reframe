interface:
  display_name: "Reframe"
  short_description: "Repo-specific patterns and workflows for Reframe"
  default_prompt: "Use the Reframe repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true