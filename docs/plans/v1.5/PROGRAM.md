# Reframe v1.5 — Program Plan (locked from owner decisions 2026-07-11)

> **Status:** ACTIVE

Synthesized from: 15-agent audit + competitor redo + live visual audit. Owner grill decisions below are FINAL scope.

## Owner decisions (final)
1. **Business model = commercialize + keep-options-open, BUILD-BUT-DON'T-WIRE.** Do ALL the NC→permissive model swaps NOW (mandatory) so commercialization stays possible; build the commercialization scaffolding but **do NOT wire it into the live app** (dormant). No live paywall in v1.5.
2. **Flagship features = ALL FOUR.** Active-speaker + multi-speaker layouts · Transcript-native editing · Local auto-B-roll · Fully-local lip-sync dub.
3. **Sequencing = keystone-refactor-first, then features (1+2+3 combined).** Land the schema-first RPC contract + typed Settings FIRST (de-risks everything), then interleave god-module splits with the feature waves.
4. **Design = FULL pro-shell redesign.** Three-zone shell + best-in-class multi-lane timeline + full IA rebuild + Figma prototypes + evolved Dark-Editorial/Signal-Amber tokens + glass layer + Cmd-K.

## Waves (execution order)

### Wave 0 — De-risk foundation (keystone-first)
- **Schema-first generated RPC contract + typed `Settings`** (retires the hand-mirrored method surface, stale CONTRACTS.md, key-injection allowlist, untyped-settings silent-no-op — one source of truth both sides generate from). *The surface size deliberately is NOT stated here: this line said `123-method` until 2026-08-11, which was 73% of the real count and is the exact literal Wave 0's own eng-day estimate was mis-sized from. It is measured and test-pinned in `docs/rpc-contract-v2.md` §1; read it there.*
- Sidecar-crash → terminal job event (no more forever-spinning job UI).
- Real cancel + process-tree teardown (Popen+should_cancel on reframe; Job Object/taskkill /T).
- Extend osv gate to torch/GPU/chatterbox lockfiles + unify torch → 2.11.
- Wire ONE real e2e + stdio-RPC contract test into the PR gate (xvfb golden-journey w/ real audio fixture).
- Split the 3 worst god-modules (main.ts, Services, _export_one).

### Wave 1 — Trust / distribution
- ~~Authenticode-signed + attested CI release pipeline (tag-triggered release.yml, pin 4 build SHA-256s, SBOM/provenance, release depends on `quality`).~~
  **REVERSED 2026-08-09 by `docs/plans/v1.5/GRILL-DECISION-QUEUE.md` §F-1:** *"Signing stays NONE (already by design). No SmartScreen concern to solve."* The absent `release.yml` is therefore CORRECT, not an outstanding item. Design docs marked SUPERSEDED.
- Electron 39 (EOL) → 43 + ASAR-integrity fuses + Electronegativity CI. **PARTIALLY SHIPPED — the line
  used to read "SHIPPED" on the strength of the version bump alone.** Corrected 2026-08-10 after
  measurement: (a) the bump DID land, the app runs Electron 43; (b) the **fuses did not** —
  `electron-builder.yml` declared no `electronFuses` block at all, so the shipped exe kept
  `NODE_OPTIONS`/`--inspect` open and would load a tampered `app.asar`. That is now fixed and pinned by
  `.quality/electron_hardening_check.py`, which also pins the `webPreferences` sandbox triple (that had
  no assertion anywhere). Note `runAsNode` stays **true** on purpose — the caption render path spawns the
  Electron exe as plain Node. (c) **Electronegativity is still NOT wired**, deliberately, with the
  reasoning and the revisit condition recorded in `QUALITY-CHARTER.md`. The fuse *bits in a built exe*
  remain UNVERIFIED here — the gate reads config; agents do not run builds. Settling experiment is named
  in the charter note.
- **Licensing swaps (mandatory per decision 1):** ViNet-S (CC-BY-NC-SA) → UNISAL (Apache-2.0); NLLB/Seamless (NC) → MADLAD-400; verify Parakeet/EdgeTAM permissive. Behind existing backend seams. **Annotation added 2026-08-11 (W24 remediation) — the decision text above is left VERBATIM; only its WORDING is flagged, the decision itself stands.** The pairing "ViNet-S (CC-BY-NC-SA) → UNISAL (Apache-2.0)" is itself a code→weights conflation — the same class the sub-bullets below retract twice, and it was left live by the W24 verification pass, whose claim to have found only ONE such instance is retracted in the next bullet. ViNet-S's CC-BY-NC-SA is a **WEIGHTS** licence, attached in LIVE CODE to the downloadable asset: `sidecar/media_studio/features/saliency.py:421` is the `AssetEntry.label` "ViNet-S video saliency (ICASSP 2025, CC-BY-NC-SA 4.0)" on the `installer="download"` entry, and the registering docstring at `:408` reads "LICENSE: CC-BY-NC-SA 4.0 — personal / NON-COMMERCIAL only". UNISAL's Apache-2.0 is the **REPOSITORY/CODE** licence and its weights licence is UNVERIFIED (next bullet). So the two sides of the arrow are not the same KIND of licence, and the swap's licence gain is of *unmeasured size* until the UNISAL weights terms are settled. **Scope of this annotation, stated narrowly:** `docs/THIRD-PARTY-LICENSES.md:89`'s `Weights licence | Code licence` SSOT table has **no ViNet-S row** — it holds only LatentSync and MuseTalk — so the weights reading rests on those two `saliency.py` anchors plus `docs/THIRD-PARTY-LICENSES.md:174` describing ViNet-S as *bundled*, not on that table.
  - **Translator: DONE, by a different model than planned.** The NC translator was replaced with **Apache-2.0 Qwen3** (tier1 Qwen3-4B / tier2 Qwen3-8B), not MADLAD-400 — the licence objective was met, the named model was not used. Recorded here so the plan stops reading as outstanding. If MADLAD-400 was wanted *specifically*, that is a new decision.
  - **ViNet-S → UNISAL: NOT DONE. The WEIGHT needs a ONE-file maintainer re-host; the LOADER half was never blocked and was not attempted here.** Measured 2026-08-10, re-measured and corrected 2026-08-11 (W24 remediation — this headline and six of the eight sub-bullets carried claims REFUTED on re-measurement; each is rewritten in place with the retraction recorded, not silently deleted). **A THIRD pass on 2026-08-11 — the W24 VERIFICATION lane, which had never run because the previous agent died mid-pass — re-derived the three contested adjudications from scratch instead of trusting them:** the licence code-vs-weights split, the `commercial`-flag install-layer drop, and the `torch.load` code path were each independently re-measured, with detector controls. **Two overclaims in that pass's own summary sentence were REFUTED by a FOURTH pass on 2026-08-11 (W24 remediation) and are corrected here, not deleted.** (1) Previous wording, REFUTED: "every bullet below now states inline what CONFIRMED, what was reproduced a second time, and the one number set this lane could NOT reproduce" — measured, that pass touched only lines 39, 40, 41, 42 and 46 of the eight sub-bullets that live at `:40-47` (`git diff origin/main...HEAD -U0 -- docs/plans/v1.5/PROGRAM.md` reports exactly two hunks, `@@ -39,4 +39,4 @@` and `@@ -46 +46 @@`), so **four of the eight received ZERO new text and were NOT re-measured by it** — no-safetensors (`:43`), why-no-code-shipped (`:44`), API-delta (`:45`) and still-open (`:47`); and even read distributively the sentence fails, because "the one number set this lane could NOT reproduce" appears in exactly ONE bullet (`:42`) while `:41`'s edit is a retraction rather than a confirmation. Correctly scoped: the THREE bullets carrying the contested adjudications — licence (`:40`), second-pickle (`:41-42`), commercial-flag (`:46`) — each state inline what that pass CONFIRMED, and `:42` additionally states the number set it could not reproduce. (2) Previous wording, REFUTED: that pass "found **one** residual code→weights conflation still live in the next bullet and retracted it there" — it did retract that one, but **two more were live and unretracted**: this item's own PARENT bullet at `:37`, the more load-bearing instance because it is the locked decision a reader quotes, and the corpus index at `docs/plans/v1.5/README.md:46`, which quotes the same pairing right after `:44-45` has already named ViNet-S's side "the CC-BY-NC-SA checkpoint". Both are annotated as of this pass. UNISAL was never integrated; the tree still gates ViNet-S off for a commercial build. Every `unisal/…` and `training_runs/…` path below is UPSTREAM, pinned at `rdroste/unisal@0440df77` — **nothing UNISAL is vendored into Reframe** (`git ls-files` returns zero matches for `unisal|training_runs|pretrained`, against 6 for a `saliency` control, so that zero is not a broken matcher). Findings, each with the probe that produced it:
    - **The CODE licence is Apache-2.0; the WEIGHTS licence is UNVERIFIED.** Two mechanically independent probes: the GitHub `/license` endpoint reports SPDX `Apache-2.0`, and the raw `LICENSE` blob (11,357 bytes, git blob `261eeb9e`) is the verbatim Apache License 2.0 text. **Both measure the repository/CODE licence only.** The earlier wording — "the plan's licence premise is CONFIRMED" — was REFUTED as wider than its evidence: this project's own SSOT keeps `Weights licence` and `Code licence` as SEPARATE columns (`docs/THIRD-PARTY-LICENSES.md:89`), and the house convention reasons explicitly about training-data encumbrance before calling weights commercial-OK (`sidecar/media_studio/assets/manifest.py:587-592`, the EdgeTAM/SA-1B case). UNVERIFIED: whether the UNISAL *weights* are commercially usable — the pinned `training_runs/pretrained_unisal/UNISAL.json` declares `"sources": ["DHF1K", "Hollywood", "UCFSports", "SALICON"]`, four research datasets whose terms were never read in this lane. Settling experiment: read each of those four licences and record, per dataset, whether it governs raw data only or also trained weights — the analysis already written down for EdgeTAM. Until that is done the swap is a licence improvement of *unmeasured size*, not a proven licence-clean move. **Re-probed 2026-08-11 (W24 verification lane — the pass that never ran), and the gap is now POSITIVELY measured rather than merely unexamined:** the upstream `README.md` at the pinned commit (4,671 B, 127 lines) contains **zero** occurrences of `licen`, and the detector is controlled on that same scan — `weight` (1 hit), `dataset` (4), `DHF1K` (6) and `SALICON` (5) all fire — so the zero is a real absence, not a broken matcher. Its ONE mention of the weights (`README.md:57`) is purely operational ("The predictions are generated with the pretrained weights in *training_runs/pretrained_unisal*") and grants nothing, and the four datasets are named as third-party downloads the user fetches from their own sites (`README.md:76-95`). **Two claims that stood here were REFUTED on 2026-08-11 (W24 remediation) as wider than their evidence — both erring in the REASSURING direction inside the one bullet whose job is to keep the dataset→weights encumbrance question OPEN — and are corrected, not deleted.** (a) Previous wording, REFUTED: the datasets "appear **only** as third-party downloads … (`README.md:76-95`)" — a re-scan of that same pinned README finds dataset names on **four lines OUTSIDE that range**, namely `:27` (the DHF1K benchmark link), `:56` (the demo-examples sentence) and `:105-106` (the eval-set description), against eight inside it. (b) Previous wording, REFUTED: "never redistributed here" — upstream itself links AUTHOR-HOSTED Google-Drive and Baidu-Pan archives of "example files from the DHF1K, Hollywood-2, UCF-Sports, SALICON and MIT1003 datasets" (`README.md:56`, `:60-62`), and the pinned tree DOES ship dataset-DERIVED index artifacts: `unisal/cache/test_hollywood_register.json` (286,120 B), `train_hollywood_register.json` (217,745 B), `test_ucfsports_register.json` (4,019 B — a map of UCF-Sports clip names, head `{"vid_name_dict":{"1":"Diving-Side-001",…`), `train_ucfsports_register.json` (8,751 B), `unisal/dhf1k_n_images.dat` (4,038 B), `unisal/cache/img_size_dict.json` (34,001 B), plus four per-dataset configs under `training_runs/pretrained_unisal/`. Correctly scoped: no dataset MEDIA is shipped in-repo (`examples/` holds one 273 B `README.md` and nothing else), but excerpts ARE redistributed off-repo by the author and derived indexes ARE in-repo — which BROADENS the dataset-terms question and therefore STRENGTHENS, not weakens, the UNVERIFIED verdict above. **A RECURSIVE tree scan** of the pinned commit (GitHub trees API, `recursive=1`, `truncated: false`, 41 entries) returns exactly one licence artifact — root `LICENSE`, 11,357 B, blob `261eeb9e`, independently corroborating the byte count taken from the blob — and **no** weights-specific licence file beside either weight: `training_runs/pretrained_unisal/` holds 8 files (the two `.pth` checkpoints plus six JSON configs) and `unisal/models/weights/` holds 1 (`mobilenet_v2.pth.tar`), none of them a licence. Detector control on that same scan: the same matcher against `README` returns `['README.md', 'examples/README.md']`, so the single hit is a real absence and not a broken matcher. **Previous wording, REFUTED:** that conclusion was drawn from "a ROOT listing", which cannot see a single subdirectory and is fully consistent with a per-weight `LICENSE` sitting beside the checkpoint — the conclusion held, but the cited probe could not support the word "anywhere". So upstream asserts NO weights licence anywhere in the pinned tree: the encumbrance question is genuinely OPEN upstream, not merely unread by us. **None of this is legal advice** — it is a reading of licence text and repository metadata by an engineer, and any actual commercial decision needs a lawyer.
    - **The weight ships as a pickle, and Reframe's load path structurally refuses it.** `training_runs/pretrained_unisal/weights_best.pth` (15,368,928 bytes, blob `f06b52c5`) is in the UPSTREAM repo, whose only licence artifact is that Apache-2.0 `LICENSE` — **but this line no longer says the weight is COVERED by it. Previous wording, REFUTED 2026-08-11: "is in the UPSTREAM repo *under that Apache-2.0 CODE grant*"** — that is precisely the code→weights inference the bullet above refutes, reproduced two sentences after refuting it, so it is retracted here rather than left to be quoted back as settled. Whether a repo-level grant reaches these particular bytes is the open question, not a premise. Size and blob sha were re-measured on the raw download, with `git hash-object` on the downloaded bytes returning the same `f06b52c5…` independently of the GitHub metadata. But `media_studio/features/_safetensors_loader.py::assert_safetensors_path` permits `.safetensors` ONLY and refuses `.pth` LOUD — a live assertion, not a comment: mutating that gate to accept `.pth` turns `sidecar/tests/test_safetensors_loader.py` RED on three cases (`[weights.pth]`, `[x.safetensors.pth]`, and `test_load_state_dict_refuses_non_safetensors_before_read`). Widening it would re-open the pickle-RCE surface WU I2 closed.
    - **The SECOND pickle is AVOIDABLE — now measured; the previous UNVERIFIED was a false blocker.** The pinned `training_runs/pretrained_unisal/UNISAL.json` sets `cnn_cfg.pretrained: true`, and on that DEFAULT path `unisal/models/MobileNetV2.py:184-195` runs `torch.load()` itself against the upstream-vendored `unisal/models/weights/mobilenet_v2.pth.tar` (14,205,652 bytes, blob `3e46cffe`) — inside architecture code, so it bypasses the verify-before-load gate entirely, contradicting `docs/plans/v1.5/model-rehosting.md`'s rule to delete `torch.load` outright. But that `torch.load` sits inside `if self.pretrained:`, and `unisal/model.py:206` binds `self.cnn = MobileNetV2(**self.cnn_cfg)` — so Reframe controls it at construction. The previous wording left the `pretrained=False` escape UNVERIFIED "because it needs torch plus the real weight to try": **REFUTED — torch 2.11.0+cpu was installed the whole time** (two independent probes: a direct import, and `pip show torch` reporting `Location: C:\Python314\Lib\site-packages`) and the weight is a public raw download. Running the settling experiment this doc itself named: `weights_best.pth` is a flat 835-key `str→Tensor` map (0 non-tensor values) holding 312 `cnn.*` entries; the module built from the pinned `cnn_cfg` with `pretrained=False` has exactly 312 `state_dict` keys; the diff is **0 missing / 0 unexpected / 0 shape mismatches** and a STRICT `load_state_dict` SUCCEEDS. Mutation control: the identical comparison against `rnn.*` reports 312 missing and goes RED, so the zero is not a vacuous detector. **`mobilenet_v2.pth.tar` therefore never needs to be loaded, converted or hosted — this is ONE re-host, not two.** **Independently re-verified 2026-08-11 (W24 verification lane).** Torch: CONFIRMED present by two mechanically independent probes — `import torch` reports `2.11.0+cpu` running from `C:\Python314\python.exe`, and `pip show torch` reports `2.11.0` at `C:\Python314\Lib\site-packages` (`safetensors 0.7.0` likewise) — so the original "cannot be run: no torch in this environment" was simply false, and the refutation above stands on a second pair of probes rather than on one agent's word. The CODE PATH was then re-derived statically from the pinned upstream, fetched over two mechanically different transports (the GitHub contents API and a raw `raw.githubusercontent.com` download) that agree, and **every cited line resolves EXACTLY**: `MobileNetV2.py:126` declares `pretrained=True` as an ordinary constructor kwarg, `:184` is `if self.pretrained:`, `:185` is the `torch.load(`, `:195` is the `else:` branch's `self._initialize_weights()`, `model.py:206` binds `self.cnn = MobileNetV2(**self.cnn_cfg)`, and `UNISAL.json` does carry `cnn_cfg.pretrained: true`. So on the pinned config the second `torch.load` IS reached, and a `pretrained=False` construction structurally never reaches it — both halves confirmed with no weight downloaded and no pickle executed. **A weight-free corroboration is newly added for the "ONE re-host" conclusion:** upstream's own shipped load path `model.py:39-42` (`load_best_weights`) calls `self.load_state_dict(torch.load(… weights_best.pth …))` with **no `strict=False`** — a STRICT load of that checkpoint into the whole UNISAL module, which cannot succeed unless the checkpoint already carries every `cnn.*` key. **Scope limit, stated INLINE and not at the end — and itself CORRECTED on 2026-08-11 (W24 remediation). Previous wording, REFUTED for mis-attributing the blocker: "this lane did NOT reproduce the 835-key / 312-key / 0-mismatch tensor comparison — that needs the weight bytes".** The **312-key MODULE side needs no weight bytes at all**, so lumping it into one "needs the weight bytes" clause left a figure marked unreproduced-in-principle that is reproducible in about two seconds; it has now been reproduced independently and weight-free. Loading the pinned upstream `MobileNetV2.py` by file path and constructing it with the pinned `cnn_cfg` (`{"widen_factor": 1.0, "pretrained": true, "input_channel": 32, "last_channel": 1280}`) but `pretrained` flipped off yields **exactly 312** `state_dict` keys, all 312 prefixed `features.`, with nothing downloaded and no pickle de-serialised. The same run upgrades this bullet's `torch.load` reading from a static line-read to an EXECUTED measurement: with `torch.load` monkeypatched to COUNT invocations, the pinned `pretrained=True` path invokes it **once** (against `…/weights/mobilenet_v2.pth.tar`) and `pretrained=False` invokes it **zero** times, while both arms produce the identical 312-key set. Detector control on the key counter: `len(nn.Conv2d(3,3,3).state_dict())` = 2 and the first five feature blocks alone = 72, so 312 is not hardwired — but recorded honestly, `widen_factor=0.5` and `input_channel=16` ALSO return 312 (they move tensor SHAPES, not the layer topology that sets the key count), so those two knobs are NOT valid controls and were discarded. Still NOT reproduced here and genuinely weight-gated: **the 835-key checkpoint side and the 0-missing / 0-unexpected / 0-shape-mismatch diff**, which remain single-sourced to the 2026-08-10 pass, so "ONE re-host, not two" rests on those plus the module-side 312 (now twice-measured) plus the strict-load inference just given. Settling experiment for whoever re-runs it: fetch the pinned `weights_best.pth` and open it with `torch.load(..., weights_only=True)` — a restricted unpickler, so key-set comparison does NOT require executing an untrusted pickle — then re-diff against a `pretrained=False` module.
    - **No safetensors UNISAL exists to point at.** Scoped search: the Hugging Face Hub holds exactly one UNISAL artifact, `litert-community/UniSal-Saliency-LiteRT` — a 6,532,768-byte `unisal_fp16.tflite`, a different runtime family and a third-party conversion of unmeasured fidelity — and the project's own re-host repo `Prekzursil/reframe-asd-weights` contains only `vinet-s-saliency.safetensors` and `transnetv2.safetensors`. So the ONE file that is actually needed (`weights_best.pth`) must be converted and hosted first — the earlier "both pickles" reading is REFUTED by the 312/312 key measurement above — **which inherits that bullet's scope limit, added here 2026-08-11 (W24 remediation) because this line asserted the number with no caveat at all: the 312-key MODULE side is reproduced weight-free by two independent passes, but the 835-key CHECKPOINT side and the 0-mismatch diff are still single-sourced to 2026-08-10 and weight-gated.** The "both pickles" refutation does not actually depend on the checkpoint side: it follows from the module-side 312 plus the measured fact that a `pretrained=False` construction invokes `torch.load` zero times.
    - **Why no code shipped — and the half that was NOT blocked.** `docs/plans/v1.5/model-rehosting.md` rules that converting a pickle is "a HOSTING-time, offline, trusted step — NEVER runtime", performed by the maintainer, who then captures the pinned commit and the sha256 of the HOSTED bytes; a manifest `installer="download"` entry cannot be registered without that real sha256 (`sidecar/media_studio/assets/manifest.py:169-172` raises without a 64-hex pin), and inventing one yields an asset that can never install — saliency permanently degraded, i.e. the exact defect this item exists to fix. That much stands. **REFUTED, by the very authority this bullet cites:** "landing the architecture would be the forbidden half-swap, so nothing was committed" is not what the re-host workflow says — `docs/plans/v1.5/model-rehosting.md:262` rules the LOADER stream "Developable against a locally-converted file — does not need the hosted URL → fully parallel to HOST", with manifest registration (`:257`, G-5a) landing LAST. So the loader/backend rewrite WAS developable inside this lane and simply was not attempted; the real constraint is only that it must not flip the user-visible asset ahead of G-5a. The offline convert is measured hazard-free: `safetensors.torch.save_file` accepts the raw 835-key state dict with no flattening and no `.contiguous().clone()` — so the full-checkpoint hazard at `docs/plans/v1.5/model-rehosting.md:251` does not apply to this weight — and a round-trip `torch.equal` holds on all 835 tensors with dtypes preserved (mutation control: perturbing one tensor reports the mismatch, so the equality check can fail) — **but that 835 is the SAME weight-gated number the second-pickle bullet scopes as unreproduced, and this sentence carried NO caveat until 2026-08-11 (W24 remediation). That is the defect being fixed: one bullet scoped 835 as unreproduced while this one, two bullets later, asserted it as measured fact and framed the convert as "measured hazard-free" — a self-contradiction inside a single bullet list. Correctly scoped: the convert is measured hazard-free ON THE 2026-08-10 PASS ONLY — single-sourced, requiring the weight bytes, and NOT reproduced by either later pass; the settling experiment is the one named in the second-pickle bullet.** Deliberately NOT recorded here: the digest of that LOCAL conversion. The manifest pin must be the sha256 of the HOSTED bytes, and a local digest sitting in a plan doc invites exactly the wrong paste.
    - **API delta, for whoever resumes:** upstream `unisal/model.py:479-487` — `UNISAL.forward(x, target_size=None, h0=None, return_hidden=False, source="DHF1K", static=None)` takes `[B,T,C,H,W]` and returns `[B,T,1,H,W]` log-softmax maps, against ViNet-S's `[1,C,T,H,W]` → single map. A real rewrite of `saliency_backend.ViNetSaliencyBackend.infer`, but a tractable one behind the existing `SaliencyBackend` seam — the seam itself needs no change.
    - **Correction to this line's previous wording — and a correction to that correction.** The original line said a commercial build "silently ships worse crop quality". The **"silently" half is REFUTED**: the advisor block is LOUD — `system_advisor.py:398-399` tests `commercial and not spec.license_commercial_ok` at `:398` and returns `spec.reason_block` at `:399` ("CC-BY-NC-SA 4.0 non-commercial; local-only — dropped for commercial build"), which the UI renders as a reason plus a `Local-only` chip (`advisorMeta.ts::licenseChip`) and an explicitly-worded disabled button ("Blocked by a non-commercial license in this build.", `app/renderer/src/components/ModelCard.tsx:66-73`), a behaviour PINNED by `app/renderer/src/components/advisorComponents.test.tsx:217-232`. But the **"worse crop quality" half STANDS, and W24's own replacement wording ("not a quality degrade") is itself REFUTED** — at the INSTALL layer, measured end to end (`advise` → `_advisor_report_to_wire` → `recommend`): `commercial=False` proposes `['hsemotion-onnx', 'rapidocr-onnx', 'vinet-s-saliency']` and `commercial=True` proposes `['hsemotion-onnx', 'rapidocr-onnx']`. The flag drops it because the advisor's `unavailable` verdict is exactly `sidecar/media_studio/features/recommender.py:107`'s `_NOT_RUNNABLE` and `:252-253` skips it; the `commercial=False` run is the detector control, so the drop is not vacuous. A commercial build therefore cannot install ViNet-S from the Models panel and is never offered it by the recommender, so its reframe crop runs YuNet-only — measurably worse than the same machine with the flag off. What is genuinely ungated is the RUNTIME: `features/saliency.py::compute_saliency_signals` never reads `commercial`, so a user who already has the weight keeps full ViNet-S quality — a residual NC-licence exposure. **So the exposure is install-AND-runtime, and the two surfaces CONTRADICT each other:** `sidecar/media_studio/handlers/_capabilities.py:132-133` still emits `{"kind": "assets.ensure", "assets": ["vinet-s-saliency"]}` for `reframe.saliency`, and its only call site `sidecar/media_studio/handlers/library_ops.py:662` passes no `commercial` at all — so the readiness rollup hands the user a one-button install of the very NC weight the Models panel refuses. That contradiction is a REAL defect and is **NOT fixed here** (it needs `commercial` threaded through `feature_readiness_items` under TDD, in a lane that owns those handler files); it is carried into "Still open" below rather than left implicit. Whether the runtime non-enforcement is deliberate (the advisor is advisory by design) is a design call left open, and the licence characterisation is a reading of licence text, not legal advice. **Independently reproduced 2026-08-11 (W24 verification lane), so the "worse crop quality" half is now measured twice by different hands:** re-running the `advise` → `_advisor_report_to_wire` → `recommend` chain from a clean interpreter returns exactly the stated sets — `commercial=False` → `['hsemotion-onnx', 'rapidocr-onnx', 'vinet-s-saliency']`, `commercial=True` → `['hsemotion-onnx', 'rapidocr-onnx']`, set difference `{'vinet-s-saliency'}` — with the saliency verdict flipping `ok` → `unavailable` and its reason string reading `CC-BY-NC-SA 4.0 non-commercial; local-only — dropped for commercial build`. Every file:line anchor in this bullet was re-resolved and all hit exactly (`system_advisor.py:398-399`, `recommender.py:107` and `:252-253`, `ModelCard.tsx:66-73`, `advisorComponents.test.tsx:217-232`, `_capabilities.py:132-133`, `library_ops.py:662`), and the "`saliency.py` never reads `commercial`" claim was re-checked with a CONTROLLED grep — that file does contain `commercial` hits, but none of them is code, so the matcher demonstrably fires and the absence of a runtime gate is real. **The COUNT in that sentence was itself wrong and is corrected here (2026-08-11, W24 remediation). Previous wording, REFUTED: "three `commercial` hits, but all three are comment/docstring prose (`:26`, `:54`, `:216`)".** That grep was case-SENSITIVE and therefore structurally blind to `NON-COMMERCIAL` (uppercase) at `:408`, inside the `register_saliency_assets()` docstring — the exact broken-detector class the sentence invoked to establish its own credibility, since control terms firing on lowercase say nothing about the case-fold actually being missed. Correctly scoped: **four** `commercial` hits case-INSENSITIVELY (`:26`, `:54`, `:216`, `:408`) against three case-sensitively, and the qualitative conclusion is unchanged but now measured by a tokenizer instead of by eye — Python's `tokenize` classifies them as COMMENT `{54, 216}` and STRING/docstring `{26, 408}` with the CODE bucket **empty**. That classifier is controlled on the same file (it puts `compute_saliency_signals` in CODE at `:223` and `register_saliency_assets` in CODE at `:398` and `:436`), so the empty CODE bucket is a real absence rather than a dead classifier, and "zero code occurrences" is now a mechanical result rather than an eyeball verdict. **A detector trap worth recording, because it silently inverts this result:** the first re-run reported the asset absent from BOTH arms and was therefore VACUOUS — `vinet-s-saliency` is registered LAZILY at `features/saliency.py:436`, so without importing `media_studio.features.saliency` the manifest lookup returns `None` and `_derive_downloads` skips it for a reason that has nothing to do with the licence flag. Import that module first, and confirm the `commercial=False` control actually proposes the asset, before reading anything into the `commercial=True` arm.
    - **Still open — four separable items, not one blob.** The suite remains green because the swap did not happen (`sidecar/tests/test_handlers_phase8.py:242::test_system_advisor_commercial_flag_drops_noncommercial` asserts the un-swapped state), which is correct today and must be changed deliberately when the swap lands.
      1. **Settle the WEIGHTS licence first** (licence bullet above). Hosting an encumbered weight would only move the problem; this gates item 2.
      2. **Owner action — the ONLY genuinely owner-gated step; its ONE-file premise is PART-UNREPRODUCED and weight-gated.** (Tag added 2026-08-11, W24 remediation: this recommendation rests on the 835-key premise that the second-pickle bullet scopes as unreproduced, and per the honesty contract a recommendation resting on an unreproduced premise inherits the tag in its HEADING — it previously carried none.) Convert `weights_best.pth` (ONE file; the earlier "both pickles" instruction was REFUTED above — on the module-side 312-key count, which two passes now reproduce weight-free, plus the measured zero `torch.load` invocations on `pretrained=False`. The 835-key checkpoint side that would make "ONE file" airtight is still single-sourced to 2026-08-10, so verify the key sets during the convert before treating it as settled — the convert opens the checkpoint anyway, making that check free) to safetensors offline, prove tensor equality, host it, and hand back the pinned commit plus the sha256 of the HOSTED bytes. The identical convert→prove→host→pin round is already executed in this repo for ViNet-S itself — `sidecar/media_studio/features/saliency.py:379-395` carries a 40-hex HF commit pin plus a hosted-bytes sha256 — so this is a precedented one-file maintainer step, not an external wall.
      3. **Developable NOW, not owner-gated.** The vendored architecture + `SaliencyBackend` rewrite + `pretrained=False` construction, built against a locally-converted file per `docs/plans/v1.5/model-rehosting.md:262`, converging on the manifest entry last.
      4. **The readiness/Models-panel contradiction** from the bullet above: `_capabilities.py` offers an `assets.ensure` install of the CC-BY-NC-SA `vinet-s-saliency` weight in a commercial build while `ModelCard` refuses it. Independent of this swap — it should be fixed whether or not UNISAL ever lands.
  - **Forced aligner (not in the original list):** `MahmoudAshraf/mms-300m-1130-forced-aligner` is CC-BY-NC-4.0 and was shipping **undisclosed**. Owner ruling 2026-08-09: disclose it + implement the `allowNonCommercialAligner` opt-in; do NOT swap the default.
- **Commercialization scaffolding, BUILD-BUT-DON'T-WIRE:** licence/edition model, feature-flag gates, edition config — all dormant, no live paywall.

### Wave 2 — Full pro-shell redesign (design track, runs parallel from now)
- Figma prototypes (transpose current app → redesigned three-zone shell + multi-lane timeline).
- IA restructure: collapse the 16+ editor tabs → ~5 phases (Transcribe → Edit → Reframe → Caption → Export); unify the inconsistent nav/brand/top-bar; content-first Library (video grid, readiness demoted).
- Token evolution: surface ladder (never pure #000), glass layer (backdrop-blur floating surfaces), bundled Inter/Geist + IBM Plex Mono + Newsreader (self-hosted, CSP), Cmd-K command palette, motion.
- Fix the WCAG-A hard barrier (mouse-only Timeline → keyboard model) as part of the rebuild.

### Wave 3 — The 4 flagship features (TDD each)
- **Active-speaker + multi-speaker layouts** (NVIDIA ASD NIM local; dynamic cut-to-speaker + 9:16 host/guest split).
- **Transcript-native editing** (delete words → delete video; over existing cues/fillers/silencetrim).
- **Local auto-B-roll** (transcript → concept spans → semantic-match user's OWN indexed library → cutaways).
- **Fully-local lip-sync dub** (translate → TTS → MuseTalk/LatentSync opt-in GPU tier).

### Wave 4 — Close-the-loop + perf + polish
- Reframe trust loop: crop preview + wire the orphaned `ReframeOverridePanel` + confidence badge.
- Viral caption engine (emoji-burst, inline keyword-pop, SFX-on-emphasis, animated word-art, auto-zoom).
- Non-destructive per-clip re-edit; real per-model token pricing (fix the spend-cap "theater").
- GPU encode (nvenc/qsv) + decode-once pipeline + Whisper VAD + Library virtualization.
- Interrupted-job recovery tray + renderer async-error handler + global JobProvider.
- Onboarding coach + progressive ShortMaker disclosure; platform-ready delivery (auto-metadata + one-source→every-platform).
- P2 hardening/modernization opportunistically (CSP, MediaPipe Tasks-API, React 19, FFmpeg-8 AV1, macOS decision-gated).

## Reality note
This is a **major release** (weeks of engineering, not one session). Execution is checkpointed wave-by-wave with fresh adversarial review + the 100% coverage gate per work unit. Design track (Wave 2) runs in parallel from now since it's independent of the refactor.

## Deferred / decision-gated (still open)
- macOS/Apple-Silicon target (owner deferred to a later call — Windows-x64 for v1.5 unless reprioritized).
- Signing route (Azure Trusted Signing vs interim detached-signature) — pick during Wave 1.
- Which flagship ships first within Wave 3 (all 4 are IN; ordering TBD — recommend active-speaker or transcript-editing first as highest-leverage).
